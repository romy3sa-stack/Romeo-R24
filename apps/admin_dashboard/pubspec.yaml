name: receipt24_admin
description: Receipt24 Super Admin Dashboard — platform administration.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  receipt24_shared:
    path: ../../packages/receipt24_shared
  flutter_dotenv: ^5.1.0
  google_fonts: ^6.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
