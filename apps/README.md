# Application Boundaries

- `consumer`: future mobile-first consumer app
- `accountant-portal`: future responsive accountant portal
- `admin-dashboard`: future restricted administrator dashboard

These directories intentionally contain no dashboard implementation in Phase 1–2. All clients will share the database authorization rules; no client is trusted to enforce access by itself.
